// Generated with util/create-component.js
import { IProps } from "../../interfaces/IProps";
import { ReactElement } from "react";

export interface TitleArrowProps extends IProps {
    title: string;
    subTitle: string;
    children?: ReactElement;
    spaceTop?: boolean;
}